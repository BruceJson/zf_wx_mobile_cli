import ajax from '@/utils/ajax';

const fetch = ajax.doNet;
const formFetch = ajax.doFormNet;

export default {
    // todo 在这里定义api接口


    // // 获取绑定的用户token
    // getBindUser(data) {
    //     return fetch({
    //         url: '/wechat/user/getuserid',
    //         method: 'post',
    //         data
    //     })
    // },
}
